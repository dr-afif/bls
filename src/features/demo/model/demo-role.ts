export type DemoRole = "learner" | "instructor" | "administrator";

export const demoRoleLabels: Record<DemoRole, string> = {
  learner: "Learner",
  instructor: "Instructor",
  administrator: "Administrator",
};
